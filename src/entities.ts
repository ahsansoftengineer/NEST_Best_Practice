import { Employee } from 'feature/employee/entities/employee.entity';
import { Person } from 'feature/person/entities/person.entity';
import { Student } from 'feature/student/entities/student.entity';
import { Teacher } from 'feature/teacher/entities/teacher.entity';

export const entities = [Person, Employee, Student, Teacher];
